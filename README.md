# modelium
Modelium is a System Dynamics sandbox to build causal graphs with signed, weighted links, set initial node values, and run/pause simulations over time. Explore feedback loops and what-if scenarios, then export run summaries.
